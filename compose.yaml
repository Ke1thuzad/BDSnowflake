services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: admin
    ports:
      - "5433:5432"
    volumes:
      - ./data:/data
      - ./scripts:/docker-entrypoint-initdb.d/
